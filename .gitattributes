# Preserve exact source bytes used by verification and provenance hashes.
# Accept both existing LF and CRLF line endings while checking other whitespace.
* -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
